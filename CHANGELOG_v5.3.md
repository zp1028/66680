# v5.3 更新日志 - 全模块多算法 + 智能融合

## 🎯 核心更新

组合预测和位置号码预测现在也支持**多算法切换 + 智能融合**了，和大小单双一样有完整的算法体系。

---

## 🆕 新增功能

### 1. 组合预测：3种算法可切换

| 算法 | 图标 | 说明 |
|------|------|------|
| 经验学习 | 🎯 | 多模型集成 + EWMA自适应 + 和值交叉特征（原默认） |
| 形态匹配 | 📊 | 多长度形态匹配，统计历史相同形态的后续分布 |
| 智能融合 | 🔮 | 融合5个高级算法，4种策略可选 |

**交互**：组合预测区域顶部有3个算法标签，点击即时切换。

### 2. 位置号码预测：3种算法可切换

| 算法 | 图标 | 说明 |
|------|------|------|
| 经验学习 | 🎯 | 多模型集成 + EWMA自适应（原默认） |
| 形态匹配 | 📊 | 2/3/4期形态匹配融合 |
| 智能融合 | 🔮 | 融合5个高级算法，4种策略可选 |

**交互**：位置预测区域顶部有3个算法标签，点击即时切换。

### 3. 融合设置（精简版）

切换到「智能融合」后，每个模块下方会显示精简版融合设置：
- **融合策略**：4种策略一键切换（EWMA/等权/置信度/投票）
- **参与模型**：chip形式勾选，直观方便
- **权重分布**：迷你条形图实时显示各模型占比

### 4. 独立配置存储

- 大小单双的融合配置：独立存储（FusionConfig）
- 组合预测的融合配置：独立存储（FusionMultiConfig.combo）
- 位置预测的融合配置：独立存储（FusionMultiConfig.position）
- 三者互不影响，可以分别设置不同的策略和模型组合

---

## 📦 新增文件

### `js/predictors_multi.js` — 扩展预测器库

包含6个新预测器类和2个工厂：

| 类名 | 用途 | 对应算法 |
|------|------|----------|
| PositionExperiencePredictor | 位置-经验学习 | 复用 predictWithEWMA |
| PositionPatternPredictor | 位置-形态匹配 | 2/3/4期形态融合 |
| ComboExperiencePredictor | 组合-经验学习 | 复用 predictZuheEnhanced |
| ComboPatternPredictor | 组合-形态匹配 | 2/3期形态融合 |
| MultiLabelFusionPredictor | 多标签融合适配器 | 复用 FusionPredictor |
| PosPredictorFactory | 位置预测器工厂 | - |
| ComboPredictorFactory | 组合预测器工厂 | - |
| FusionMultiConfig | 多场景融合配置管理 | position/combo独立 |

---

## 🔧 修改文件

### `js/app.js`
- AppState 新增 `currentComboAlgo` 和 `currentPosAlgo`
- 组合预测生成逻辑重写：生成所有3种算法结果，按选中算法展示
- 位置预测生成逻辑重写：每个位置生成所有3种算法结果
- 新增 `renderComboPredMulti()` 组合多算法渲染
- 新增 `renderPositionPredMulti()` 位置多算法渲染
- 新增 `renderComboFusionSettings()` 精简版融合设置
- 新增 `renderMiniWeightBars()` 迷你权重条
- 新增 `bindComboFusionEvents()` 融合事件绑定

### `index.html`
- 引入 `predictors_multi.js`
- 版本号更新为 v5.3

### `css/style.css`
- 新增 `.sub-algo-tabs` 小节级算法切换标签样式
- 新增 `.combo-fusion-settings` 精简版融合设置样式
- 新增 `.combo-fs-btn` 策略按钮样式
- 新增 `.combo-fm-chip` 模型chip样式

---

## 📊 全模块算法对比表

| 模块 | 经验学习 | 形态匹配 | 手动标记 | 智能融合 | 切换方式 |
|------|----------|----------|----------|----------|----------|
| 大小单双 | ✅ | ✅ | ✅ | ✅ | 顶部主标签栏 |
| 组合预测 | ✅ | ✅ | ❌ | ✅ | 小节内标签 |
| 位置预测 | ✅ | ✅ | ❌ | ✅ | 小节内标签 |

> 手动标记仅大小单双支持（组合和位置手动标记意义不大）

---

## 🎨 UI 结构

```
┌─ 大小单双预测 ──────────────────────────────┐
│  [经验学习] [形态匹配] [手动标记] [🔮融合] [对比] │  ← 主标签栏
│  ┌─ 融合设置面板（仅融合模式显示）─┐        │
│  │  融合策略 / 参与模型 / 权重分布  │        │
│  └──────────────────────────────┘        │
│  ... 预测卡片 ...                           │
└───────────────────────────────────────────┘

┌─ 组合预测 ────────────────────────────────┐
│  [🎯经验] [📊形态] [🔮融合]                    │  ← 小节标签
│  ... Top3 卡片 ...                           │
│  ┌─ 融合设置（仅融合模式）─┐               │
│  │  策略按钮 + 模型chip    │               │
│  └─────────────────────┘               │
└───────────────────────────────────────────┘

┌─ 位置号码预测 ─────────────────────────────┐
│  [🎯经验] [📊形态] [🔮融合]                    │  ← 小节标签
│  ... 位置卡片 ...                            │
│  ┌─ 融合设置（仅融合模式）─┐               │
│  │  策略按钮 + 模型chip    │               │
│  └─────────────────────┘               │
└───────────────────────────────────────────┘
```

---

## 💡 使用说明

### 组合预测切换算法
1. 滚动到「组合预测」区域
2. 点击顶部的「经验学习/形态匹配/智能融合」标签
3. 切换到「智能融合」后，下方显示融合设置

### 位置预测切换算法
1. 滚动到「位置号码预测」区域
2. 点击顶部的「经验学习/形态匹配/智能融合」标签
3. 切换到「智能融合」后，下方显示融合设置
4. 所有位置共用同一个算法选择（统一切换）

### 融合设置（精简版）
- **策略按钮**：4种策略一行排列，点击切换
- **模型chip**：5个模型以标签形式展示，点击勾选/取消
- **权重分布**：实时显示各模型权重占比
- 组合和位置的融合配置互相独立

---

## 🏗️ 架构设计原则

1. **零侵入**：现有 `predictors.js` 的3个主预测器完全不动
2. **可复用**：融合引擎 `FusionPredictor` 支持任意标签数，大小/组合/位置通用
3. **独立配置**：三个模块的融合配置独立存储，互不干扰
4. **统一接口**：所有预测器遵循相同的 `predict(seq, labels, options)` 接口
5. **渐进增强**：用户不需要的话，默认还是经验学习，无感升级
