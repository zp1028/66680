# v5.2 更新日志 - 智能融合引擎

## 🎯 核心更新

### 新增：智能融合引擎（第4种预测算法）
作为独立模块新增，**完全不修改现有三个预测器代码**，零风险。

---

## 📦 新增文件

### 1. `js/advanced_models.js` — 高级算法模型库
包含5个独立算法模型，均可单独使用或参与融合：

| 模型 | 类别 | 原理 | 特点 |
|------|------|------|------|
| 🔄 马尔可夫链 | 时序模型 | 一阶状态转移概率矩阵 | 简单高效，捕捉连续关联 |
| 🔁 二阶马尔可夫 | 时序模型 | 二阶状态转移 | 使用前两期预测下一期 |
| 📐 贝叶斯平滑 | 统计模型 | Beta先验 + 后验估计 | 小样本下更稳健 |
| ⚖️ 加权频率 | 频率模型 | 指数衰减加权频率 | 近期数据权重更高 |
| 🎚️ 多尺度频率 | 频率模型 | 5/10/20/50期多窗口融合 | 兼顾短期趋势和长期稳定 |

### 2. `js/fusion.js` — 智能融合引擎
作为第4种预测器注册，支持4种融合策略：

| 策略 | 图标 | 说明 | 适用场景 |
|------|------|------|----------|
| EWMA自适应 | 📈 | 历史对错评分动态加权 | 默认推荐，数据充足时最佳 |
| 等权平均 | ⚖️ | 所有模型权重相同 | 保守稳定，数据少时 |
| 置信度加权 | 🎯 | 按各模型置信度分配权重 | 模型差异大时 |
| 一致性投票 | 🗳️ | 多数派胜出，无过半数则观望 | 求稳策略 |

---

## 🔧 修改文件

### `js/predictors.js`
- 新增 `PREDICTOR_TYPES.FUSION` 枚举
- 新增融合预测器元信息
- 在 `PredictorFactory` 中注册融合预测器
- `getAll()` 和 `getAutoPredictors()` 包含融合
- **现有三个预测器代码完全未动**

### `js/app.js`
- `generateAllAlgoPredictions()` 新增融合预测生成
- `initAlgoSelector()` 增加融合设置面板显示逻辑
- `renderAlgoComparison()` 对比面板增加融合算法
- 新增 `initFusionSettingsPanel()` 融合设置面板初始化
- 新增 `renderFusionModelList()` 子模型列表渲染
- 新增 `updateFusionWeightBars()` 权重条形图更新
- 预测生成后自动更新融合权重展示

### `index.html`
- 算法选择器新增「🔮 智能融合」标签
- 新增融合设置面板（融合策略 + 参与模型 + 权重分布）
- 引入 `advanced_models.js` 和 `fusion.js`
- 版本号更新为 v5.2

### `css/style.css`
- 新增融合设置面板全部样式（~300行）
- 支持暗黑模式
- 移动端响应式适配

---

## 🎨 新功能界面

### 融合设置面板
- **可折叠**：点击头部收起/展开
- **融合策略选择**：4种策略2×2网格布局，点击切换
- **参与模型列表**：可勾选/取消各子模型，显示分类标签
- **快捷操作**：全选 / 全不选 / 智能推荐
- **权重分布预览**：条形图实时显示各模型权重占比

### 交互流程
1. 点击算法栏的「🔮 智能融合」切换到融合模式
2. 融合设置面板自动展开
3. 选择融合策略（默认EWMA自适应）
4. 勾选/取消参与的子模型
5. 预测结果和权重分布实时更新
6. 设置自动保存到本地存储

---

## 🏗️ 架构设计

### 独立性原则
- 现有 `PatternPredictor`、`ExperiencePredictor`、`ManualMarkPredictor` 三个类**一行代码都没改**
- 新增代码完全独立，通过统一接口对接
- 出问题随时可以切回原算法，零风险

### 可扩展性
- 新增算法：往 `advanced_models.js` 加一个类，在 `ADVANCED_MODELS` 注册即可
- 新增融合策略：在 `FUSION_STRATEGIES` 注册，在 `_computeWeights` 加对应方法
- 所有配置持久化到 `localStorage`

### 数据流程
```
用户选择融合模式
    │
    ▼
generateAllAlgoPredictions 生成所有算法结果
    │
    ├─ 经验学习 ───┐
    ├─ 形态匹配 ───┤
    ├─ 手动标记 ───┤
    └─ 融合引擎 ───┘
         │
         ├─ 马尔可夫链 ──┐
         ├─ 二阶马氏 ────┤
         ├─ 贝叶斯平滑 ──┤  子模型预测
         ├─ 加权频率 ────┤
         └─ 多尺度频率 ──┘
                   │
                   ▼
            融合策略加权
            (4种可选)
                   │
                   ▼
            融合预测结果
            + 权重分布
```

---

## 📝 使用说明

1. **切换算法**：顶部算法栏点击「🔮 智能融合」
2. **选择策略**：在融合设置中选择合适的融合策略
3. **调整模型**：勾选想参与融合的子模型
4. **查看权重**：底部权重条形图显示各模型占比
5. **智能推荐**：点击「智能推荐」一键恢复推荐配置

**策略选择建议：**
- 刚使用、数据不多 → 等权平均
- 使用一段时间有战绩 → EWMA自适应（默认）
- 各模型差异很大 → 置信度加权
- 求稳、怕踩坑 → 一致性投票
